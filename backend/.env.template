# ============================================
# Water Monitoring System - Environment Variables Template
# ============================================
# 이 파일을 복사해서 .env 파일을 만들고 실제 값으로 변경하세요
# cp .env.example .env

# === 백엔드 서비스 설정 ===
DB_IP=your_db_ip
DB_PORT=3306
DB_USERNAME=your_db_username
DB_PASSWORD=your_strong_password
DATABASE=water_monitor

# === 메일 설정 ===
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password
MAIL_PROTOCOL=smtp

# === MQTT 설정 ===
MQTT_BROKER=tcp://your_server_ip:1885
MQTT_TOPIC=water/data

# ============================================
# 🔒 설정 가이드
# ============================================
# 1. your_server_ip를 실제 서버 IP로 변경
# 2. your_email@gmail.com을 실제 Gmail 주소로 변경  
# 3. your_gmail_app_password를 Gmail 앱 비밀번호로 변경
# 4. your_db_username을 실제 DB 사용자명으로 변경
# 5. your_strong_password를 강력한 DB 비밀번호로 변경